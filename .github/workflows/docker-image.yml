name: Docker Image CI

on:
  pull_request:
    branches: [ "Dev" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Build the Docker image

      
      run: docker build . --file ./BallBoi/BallBoi/Dockerfile --tag bleeding-edge-$(date +%s)
